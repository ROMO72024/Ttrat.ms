# غرس 1.1.1 — الأسماء في الأعلى والتحديث مع الحفاظ على البيانات

تأكيدك وصورة الصف 1001 أثبتا وصول الأسماء إلى الشيت. سبب موضع الاسم أن مربعات الاختيار غير المحددة تُحسب كقيم FALSE، فيرى السكربت الصف 1000 آخر صف مستخدم. تعالج هذه النسخة موضع الإدراج، وتضيف جلبًا لا يكتب قيم الهاتف إلى الشيت، وإرسال السجلات المعدلة فقط، وتوقيعًا دائمًا للتحديثات القادمة.

الحزمة مصدر مشروع كامل، وليست APK جاهزًا. لا تحتاج إلى تغيير أي ورقة أو كود أخصائية أو إعادة إنشاء الشيت.

## 1. أصلح مكان الأسماء الآن — دون تحديث التطبيق

1. افتح ملف Google Sheets الحالي الذي ظهرت فيه الأسماء.
2. اختر «الإضافات / Extensions» ← «Apps Script».
3. افتح `Code.gs` واستبدل محتواه كاملًا بملف `google-apps-script/Code.gs` الموجود في هذه الحزمة.
4. احفظ. من قائمة الدوال أعلى المحرر اختر `arrangeGharsStudents` ثم «تنفيذ» مرة واحدة. ينقل هذا الأمر صفوف البيانات الموجودة في الأسفل إلى أعلى ورقة «طلاب غرس»، مع الاحتفاظ بالأسماء والمعرّفات والصيغ والتنسيق. لا يحذف صفوف البيانات ولا يغيّر بيانات الورقة التقنية.
5. اختر «نشر» ← «إدارة عمليات النشر» ← قلم تعديل النشر الحالي ← «إصدار جديد» ← «نشر». احتفظ بعنوان `/exec` الحالي؛ لا تنشئ عنوانًا آخر.
6. أضف اسمًا تجريبيًا من تطبيقك ثم نفّذ المزامنة. سيظهر مباشرة تحت العناوين في الصف 2. الاسم التالي يظهر فوقه. تكرار إرسال السجل نفسه لا يكرر الاسم.

يمكن تشغيل الترتيب لاحقًا من قائمة «غرس · المزامنة» ← «نقل الأسماء الموجودة إلى أعلى الورقة»، بعد إعادة تحميل الشيت لظهور القائمة الجديدة.

تحديث بيانات اسم موجود لا ينقله إلى الصف 2؛ الانتقال إلى الأعلى يخص الأسماء المضافة حديثًا. إدخال المحاسب يدويًا يبقى في الصف الذي يختاره؛ لإدخاله أعلى الجدول يضيف صفًا تحت العناوين أو يستخدم أمر الترتيب لتجميع البيانات.

لا تشغّل التهيئة مجددًا لهذا الإصلاح، ولا تحذف الأوراق أو الأكواد أو عمودي المعرّفات المخفيين.

## حماية تعديل المحاسب: من 20 جلسة إلى 10

هذه الميزة تحتاج استبدال `Code.gs` ونشره، ثم بناء APK من المصدر الجديد أيضًا:

- زر «جلب تعديلات الشيت» يستقبل بيانات الشيت. الخادم يتجاهل قيم الهاتف في عملية الجلب حتى لو أرسلها عميل قديم أو غير صحيح في ذلك الطلب.
- أثناء فتح التطبيق مع اتصال متاح ومزامنة مفعّلة، يحاول جلب بيانات الشيت كل 15 ثانية. زمن الاستجابة يضاف إلى الانتظار، وفي الخلفية قد يؤخر Android التنفيذ. ليست مزامنة فورية عند غياب الإنترنت.
- حفظ تعديل على الهاتف يبدأ محاولة رفع تلقائية، ويرسل السجلات الجديدة أو المعدلة فقط. السجل غير المعدّل لا يعاد إرساله لمجرد جلب بيانات الشيت.
- إذا كان العدد 20 في الهاتف والشيت، ثم غيّره المحاسب إلى 10 دون تعديل العدد في الهاتف، يبقى 10 في الشيت ويصبح 10 في الهاتف بعد الجلب.
- إذا عدّلت المعلمة الهاتف إلى 25 وعدّل المحاسب الشيت إلى 10 في الفترة نفسها، يبقى تعديل الشيت محفوظًا وتظهر القيمتان في «مراجعة الاختلافات». اعتماد قيمة الهاتف يدويًا يرسلها في المزامنة التالية؛ اعتماد الشيت يحدّث الهاتف. لا تُحذف قيمة الهاتف المعلقة بصمت.
- الجلب يحتفظ بالأسماء الجديدة والتعديلات المحلية التي لم تُرفع بعد، ولا يمسحها لأن الشيت لم يستقبلها بعد.

اختبار على هاتفك: أضف اسم اختبار بإجمالي 20، ارفع التعديل وتحقق من نجاح المزامنة، ثم غيّر **إجمالي جلسات النطق (E)** أو **إجمالي جلسات السلوك (F)** في الشيت إلى 10. اضغط «جلب تعديلات الشيت» وتحقق من 10 في الطرفين. عدّل الهاتف في حقل آخر كرقم الهاتف، وتحقق أن العدد لا يرجع إلى 20.

أعمدة «منفّذ» و«متبقٍ» في M:P محسوبة من الجلسات والحضور؛ تعديلها يدويًا لا يغيّر الخطة ويعاد حسابها. المحاسب يعدّل الإجمالي في E أو F.

## 2. ما الذي يمكن تثبيته فوق النسخة القديمة؟

Android يحتاج معرّف التطبيق نفسه وتوقيعًا متوافقًا مع النسخة المثبتة. الإصدار هنا `versionCode=5` و`versionName=1.1.1`؛ رفع رقم الإصدار وحده لا يعالج اختلاف المفتاح.

- إذا لديك مفتاح التوقيع الذي استُخدم للنسخة المثبتة: أعد استخدامه. لا تنشئ مفتاحًا جديدًا مكانه. يلزم كذلك مطابقة معرّف الحزمة؛ النسخة الأساسية هنا `ps.ghars.sessions`.
- إذا كانت النسخة القديمة من GitHub Debug وكان مفتاحها مؤقتًا ولم يُحفظ: لا يمكن استخراج المفتاح الخاص من APK أو إنشاء بديل مطابق له. الاسم والشعار وكلمة مرور Debug المعروفة لا تكفي.
- في هذه الحالة الحزمة تجهز أيضًا نسخة مستقلة باسم «أخصائية غرس · المزامنة» ومعرّف `ps.ghars.sessions.transfer`. يمكن نقل نسخة احتياطية إليها مع بقاء التطبيق القديم مثبتًا. هي تطبيق مستقل، وليست تحديثًا مباشرًا للقديم. استخدم المعرف والمفتاح نفسيهما لتحديث هذه النسخة في المستقبل.

شرح Android: https://developer.android.com/studio/publish/app-signing#considerations

## 3. احفظ أسماء المعلمة قبل أي تثبيت

على هاتف المعلمة، من التطبيق الموجود حاليًا:

1. افتح الإعدادات ← «نسخة احتياطية مشفرة».
2. اختر كلمة مرور تحفظها، واحفظ الملف الذي ينتهي بـ `.ghars` في المستندات أو التنزيلات.
3. انسخ الملف إلى الكمبيوتر أو جهاز آخر أيضًا، واحتفظ بكلمة مروره. تقرير CSV وحده ليس بديلًا عن هذه النسخة؛ المطلوب الطلاب والخطط والجلسات والحضور والملاحظات.
4. أبقِ التطبيق القديم وبياناته كما هي حتى التحقق من الاستعادة.

الأسماء التي حُذفت من هاتف الاختبار بعد إزالة التطبيق لا تسترجعها إعادة التثبيت وحدها. يمكن استعادتها إذا كانت لديك نسخة احتياطية أو كانت قد وصلت إلى مساحة الكود في الشيت.

## 4. إعداد توقيع دائم مرة واحدة

إذا كانت أسرار التوقيع الأربعة موجودة في GitHub وتخص مفتاحك الصحيح، استخدمها كما هي وانتقل إلى البناء. لا تعِد إنشاء المفتاح.

إذا لم يكن لديك مفتاح محفوظ، أنشئ مفتاحًا دائمًا للنسخة الجديدة المستقلة:

1. على Windows مع JDK 17 متاحًا، افتح PowerShell داخل مجلد المشروع المستخرج.
2. نفّذ:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\create-signing-key.ps1
```

هذا الخيار يخص عملية PowerShell الحالية فقط. إذا ظهرت رسالة غياب JDK، استخدم تثبيت JDK الذي لديك أو ثبّت JDK 17، ثم افتح PowerShell من جديد. بديل إنشاء المفتاح بأوامر `keytool` مشروح في `docs/SIGNING.md`.

3. ينشئ السكربت داخل `Documents\GharsSigning` ملفين: `ghars-release.jks` و`github-secrets.txt`. لا يعيد الكتابة فوق مفتاح موجود.
4. افتح GitHub ← المستودع ← Settings ← Secrets and variables ← Actions ← New repository secret.
5. افتح `github-secrets.txt` وانقل كل قيمة إلى السر المطابق لاسمها:

| اسم السر | القيمة |
|---|---|
| `GHARS_KEYSTORE_BASE64` | السطر الطويل تحت الاسم نفسه |
| `GHARS_STORE_PASSWORD` | السطر تحت الاسم نفسه |
| `GHARS_KEY_ALIAS` | `ghars` |
| `GHARS_KEY_PASSWORD` | السطر تحت الاسم نفسه |

احتفظ بملفي التوقيع خارج مستودع GitHub ومع نسخة أخرى لديك؛ لا ترفعهما ضمن المشروع. ملف `.gitignore` المرفق يستبعدهما، لكن رفع الملفات يدويًا من المتصفح يتطلب اختيار ملفات المصدر فقط.

المفتاح الجديد يثبت توقيع التحديثات القادمة. لا يجعل APK الجديد متوافقًا بأثر رجعي مع توقيع المعلمة القديم.

## 5. بناء النسخة

1. حدّث ملفات المصدر في مستودعك بمحتويات مجلد `Ttrat.ms-main`، بما فيها `.github/workflows/main.yml`. لا تضع مجلد المشروع داخل مجلد آخر في المستودع.
2. رابط غرس الذي أرسلته موجود مسبقًا في `app/src/main/assets/sync-config.json`. الدخول بالكود فقط، ولا توجد خانة رابط أو زر لتعديله.
3. احفظ Commit changes، ثم افتح Actions ← Build APK. تابع التشغيل الجديد؛ لا تعِد تشغيل مهمة قديمة تستخدم ملف البناء القديم.
4. تتوقف المهمة برسالة واضحة إذا كانت أسرار التوقيع ناقصة. لم يعد ملف Debug ذي المفتاح المؤقت يُرفع للتوزيع على المعلمات.
5. بعد نجاح الفحص والبناء، نزّل `ghars-signed-apks-1.1.1` من Artifacts وفك ضغطه.

| الملف الناتج | الاستخدام |
|---|---|
| `ghars-update-1.1.1.apk` | تحديث مباشر فقط إذا كانت الحزمة المثبتة `ps.ghars.sessions` وكان توقيعها موافقًا للمفتاح الذي بنيت به |
| `ghars-parallel-1.1.1.apk` | نسخة مستقلة `ps.ghars.sessions.transfer` لنقل النسخة الاحتياطية دون حذف التطبيق القديم؛ اسمها «أخصائية غرس · المزامنة» |
| `signature-*.txt` و`package-*.txt` | معلومات التوقيع ومعرّف التطبيق ورقم الإصدار للتشخيص؛ لا تتضمن المفتاح الخاص |

لا تثبت الملفين الجديدين للتجربة على بيانات المعلمة. اختر مسارًا واحدًا وفق وجود المفتاح القديم. إذا كان معرّف النسخة القديمة `.debug` أو مختلفًا، أرسل APK القديم لفحص معرّفه وشهادته قبل محاولة تحديث مباشر؛ فحص APK لا يستخرج المفتاح الخاص منه.

## 6. نقل أسماء المعلمة عند عدم توفر التوقيع القديم

جرّب الخطوات أولًا على هاتفك باستخدام نسخة احتياطية اختبارية، ثم طبّقها على هاتف المعلمة مع إبقاء نسختها القديمة:

1. ثبّت `ghars-parallel-1.1.1.apk` دون حذف التطبيق القديم.
2. افتح «أخصائية غرس · المزامنة» وأدخل كود المعلمة نفسه. أول دخول يتطلب الإنترنت.
3. افتح الإعدادات ← «استعادة نسخة»، واختر ملف `.ghars` الذي صدّرته وأدخل كلمة مروره.
4. راجع اسم مساحة الأخصائية وعدد الطلاب والخطط والجلسات قبل قبول الاستعادة. الاستعادة تستبدل البيانات المحلية في التطبيق الجديد بعد التحقق؛ بيانات التطبيق القديم تبقى مستقلة.
5. بعد الاستعادة، تحقق من ظهور نحو 150 اسمًا وراجع عدة ملفات وجلسات وحضور. الاستعادة توقف المزامنة عمدًا لتراجع البيانات.
6. اضغط «رفع الأسماء الحالية وتفعيل المزامنة»، ثم «رفع تعديلات الهاتف» عند الحاجة. إذا كان الشيت يحتوي أسماء المعلمة منذ مزامنة سابقة، راجع أي تعارضات تظهر بدل إعادة كتابة الأسماء.
7. راقب وقت آخر مزامنة ناجحة وصفر السجلات المنتظرة، وافتح «طلاب غرس» وتحقق من الأسماء. جرّب تعديلًا بسيطًا من الشيت ثم جلبه في الهاتف.
8. احتفظ بالنسخة الاحتياطية وبالتطبيق القديم حتى اكتمال التحقق. بعد اعتماد التطبيق الجديد تجنّب إدخال جلسات جديدة في القديم حتى لا تنشأ نسختان مختلفتان، وأوقف تنبيهاته إذا تكررت المنبهات.

إذا كانت النسخة الاحتياطية مرتبطة سابقًا بحساب أو مدرسة أخرى، سيرفض التطبيق ربطها بمساحة مختلفة. استخدم كود المساحة الصحيح، ولا تغيّر المعرّفات لتجاوز الرفض.

## التحقق المنفذ وحدوده

- نجحت 40 نتيجة اختبار Node، بما فيها محاكاة 1000 صف بمربعات اختيار، وإدراج أحدث اسم في الصف 2، ورفع 150 اسمًا وإعادة الإرسال دون تكرار، ونقل الصفوف القديمة مع المعرفات والصيغ، وبقاء تعديل المحاسب من 20 إلى 10، ومنع مسار الجلب من الكتابة بقيم الهاتف.
- اختبارات Google Sheets محاكاة محلية؛ لم أعدّل الشيت الحي أو أنشر السكربت في حسابك.
- نجح 24 اختبار Java للنسخ الاحتياطي ودمج البيانات وطابور المزامنة، بما فيها نسخة 150 طالبًا وجلسة حضور والجلب دون إعادة إرسال القيمة القديمة؛ التفاصيل في `docs/TESTING.md`.
- تم تجميع ملفات Java مقابل واجهات Android 15/API 35 مع رموز موارد مؤقتة للفحص، والتحقق من صياغة Gradle. لم يُبنَ APK: تعذر تنزيل Gradle عبر بيئة Java المحلية، ولا يوجد Android SDK محلي كامل. يتولى سير GitHub فحص الموارد وLint وبناء APK والتحقق من توقيعه. سكربت PowerShell لم يُشغّل على Windows في هذه البيئة.
- لم تتم مقارنة توقيع التطبيق المثبت على هاتف المعلمة؛ لا يمكن ضمان التحديث المباشر قبل معرفة الحزمة والتوقيع وتوفر المفتاح الموافق.
