# تحديث نسخة غرس المرفقة — اقرأ هذا أولًا

هذه الحزمة مصدر كامل يستهدف **ملف app-debug.apk الذي أرفقته**. صححت هوية التثبيت، وأضفت فحصًا للتوقيع قبل إخراج APK. **لم يكتمل إصدار APK قابل للتحديث: مفتاح التوقيع القديم غير موجود في المرفقات.** الحزمة لا تحتوي مفتاحًا بديلًا، ولا تنشئه.

## نتيجة فحص الملف الفعلي

| العنصر | القيمة |
|---|---|
| الإصدار المرفق | `1.0.2-debug` |
| رقم الإصدار | `3` |
| معرّف التثبيت | `ps.ghars.sessions.debug` |
| شهادة التوقيع | `Android Debug` |
| إنشاء الشهادة | 13 سبتمبر 2026، الساعة 17:48:16 UTC |
| الإصدار المستهدف | `1.1.1`، ورقمه `5` |
| سلامة توقيع الملف | تحقق ناجح باستخدام مكتبة Android apksig 8.7.3 |

بصمة الشهادة SHA-256:

```text
da5b6e4b964ed2f0d79d045b8c6e5de505ea6a156bdf3844b67ee907cbaa049e
```

فحصت APK واحدًا فقط؛ لا توجد نسخة APK ثانية للمقارنة. ملفات الواجهة داخل الملف المرفق مطابقة لمصدر 1.0.2 الذي أرسلته سابقًا. الملف لا يتضمن بيانات طلاب المعلمة، ولا مفتاح التوقيع الخاص.

في الحزمة السابقة كان APK الأساسي يستهدف `ps.ghars.sessions`، وهو غير معرّف ملفك الأصلي. هذا خطأ في استهداف التحديث تم تصحيحه. اختلاف المعرف وحده يؤدي عادةً إلى تطبيق منفصل؛ لا يثبت سبب رسالة التعارض في الملف الجديد الذي لم يُرفق. التوقيع الصحيح شرط آخر مستقل.

## ما تم تصحيحه في المشروع

- إصدار Release يستهدف `ps.ghars.sessions.debug` نفسه، مع رقم إصدار أعلى. كلمة debug جزء من المعرف القديم؛ لا تحذفها.
- أسماء أصناف Java ومسار تخزين البيانات والنسخ الاحتياطية محفوظة. لم يتغير منطق المزامنة عن الإصدار المصحح 1.1.1.
- GitHub يفحص شهادة المفتاح قبل البناء. إذا كانت مختلفة، يتوقف ويبين البصمة، بدل إعطائك ملفًا آخر يرفض التحديث.
- بعد البناء يتحقق من توقيع APK ومعرفه ورقم إصداره قبل رفعه في Artifacts.
- هذا المسار لا يخرج تطبيقًا موازيًا. ملفات Debug المعدة للفحص ليست ملفات توزيع.

## الخطوة اللازمة الآن: إيجاد مفتاح النسخة الأصلية

إذا أنت أنشأت النسخة الأصلية **على جهازك باستخدام Android Studio**، فابحث عن `debug.keystore`. المسار الافتراضي على Windows:

```text
C:\Users\اسم_مستخدم_ويندوز\.android\debug.keystore
```

افتح PowerShell داخل مجلد المشروع المستخرج، وشغّل:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\check-original-key.ps1
```

الأداة تقرأ المفتاح الموجود فقط. لا تغيّره ولا تنشئ مفتاحًا. إن ظهر `MATCH` فهو مطابق للشهادة المطلوبة. إذا كان في مكان آخر:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\check-original-key.ps1 -KeyStorePath "D:\MyBackup\debug.keystore"
```

السكربت يستخدم اسم المفتاح وكلمة مرور Debug الافتراضيين: `androiddebugkey` و`android`. إذا كنت غيّرتهما، يلزم تزويد إعداد البناء بالقيم الفعلية؛ فشل القراءة لا يعني فقد الملف.

إذا حملت النسخة **من GitHub Actions فقط**، فالمفتاح أنشئ على جهاز البناء هناك، وليس على هاتفك. وجود ملف Debug جديد على الكمبيوتر لا يعني أنه مطابق. أرسل رابط تشغيل GitHub الذي أنتج النسخة القديمة، أو افحص مرفقات ذلك التشغيل ونسخ المفاتيح الاحتياطية لديك. المشروع الأصلي كان يرفع APK وتقارير الفحص، ولم أجد فيه خطوة لحفظ مفتاح Debug. إعادة تشغيل المهمة القديمة قد تنشئ مفتاحًا مختلفًا ولا تسترجع المفتاح السابق.

إذا فُقد المفتاح الأصلي نهائيًا، لا يستطيع تعديل الأكواد أو استخراج الشهادة من APK صنع تحديث مباشر مطابق. عندها تبقى النسخة الاحتياطية ونقلها المسار المتاح للحفاظ على البيانات. لا تحذف تطبيق المعلمة لتجربة مفتاح آخر.

## بعد العثور على مفتاح مطابق

1. حافظ على نسخة من `debug.keystore` الأصلي خارج المستودع. إذا ظهر MATCH، تستطيع تصدير إعدادات GitHub محليًا:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\check-original-key.ps1 -KeyStorePath "D:\MyBackup\debug.keystore" -ExportSecrets
```

2. ينشئ ملف `Documents\GharsOriginalSigning\github-secrets.txt` باستخدام **المفتاح الموجود نفسه**. لا يرفع شيئًا ولا يولد مفتاحًا جديدًا. انقل قيمه إلى GitHub ← Settings ← Secrets and variables ← Actions:

| السر | القيمة الافتراضية لهذا المفتاح |
|---|---|
| `GHARS_KEYSTORE_BASE64` | محتوى الملف الأصلي بترميز Base64، من ملف التصدير |
| `GHARS_STORE_PASSWORD` | `android` |
| `GHARS_KEY_ALIAS` | `androiddebugkey` |
| `GHARS_KEY_PASSWORD` | `android` |

لا تضع ملف التصدير أو المفتاح في ملفات المستودع. إذا كانت لديك هذه الأسرار بالفعل، لا تستبدلها قبل التحقق من أنها تخص المفتاح الأصلي. إذا كان المفتاح محفوظًا في GitHub فقط فلا تحتاج إخراجه؛ سير العمل يفحص شهادته هناك.

3. حدّث محتويات مشروع GitHub بمجلد `Ttrat.ms-main`، بما فيها `.github/workflows/main.yml`. لا تنشئ مجلدًا إضافيًا داخل المستودع.
4. شغّل `Build matching update`. لا تعد تشغيل مهمة قديمة تستخدم إعداد البناء القديم.
5. بعد نجاح البناء، نزّل `ghars-matching-update-1.1.1` من Artifacts، وفك الضغط. الملف المقصود هو `ghars-update-existing-1.1.1.apk`.
6. صدّر نسخة `.ghars` من التطبيق القديم واحتفظ بكلمة مرورها، ثم جرّب التحديث على هاتف الاختبار الذي عليه النسخة المرفقة، دون حذف التطبيق. راجع الأسماء والجلسات بعد التحديث قبل هاتف المعلمة.

الرابط المدرسي والشعار وإصلاحات المزامنة موجودة في المصدر. خطوات نشر Code.gs وترتيب الأسماء في [docs/SHEETS-SYNC-AR.md](docs/SHEETS-SYNC-AR.md).

## حدود التحقق

- تم التحقق من التوقيع الحقيقي للملف المرفق باستخدام مكتبة Android الرسمية apksig؛ التوقيع صالح.
- ملفات Java والواجهة وCode.gs هي نفسها التي اجتازت اختبارات الإصدار 1.1.1. التغيير الحالي في إعداد البناء وأدوات التحقق والتوثيق.
- فُحصت صياغة Gradle وYAML وأوامر shell، واختُبرت بوابة التحديث لرفض المعرف أو الشهادة أو رقم الإصدار غير الموافق.
- أداة PowerShell لم تُشغّل على Windows هنا. لا تفترض وجود المفتاح الأصلي عندك.
- لم يتم بناء أو تثبيت APK جديد؛ المفتاح الخاص غير متاح في المرفقات. مطابقة التوقيع والمتطلبات لازمة للتحديث، وتجربة الهاتف هي التحقق النهائي من التثبيت والبيانات.

مرجع Android: [توقيع التطبيقات](https://developer.android.com/studio/publish/app-signing).
