# التغييرات في 1.1.1

- الأسماء الجديدة الواردة من التطبيق تبدأ في الصف 2 من ورقة «طلاب غرس»، والأحدث في الأعلى.
- `arrangeGharsStudents` يجمع صفوف البيانات الموجودة أعلى الورقة دون حذف الأسماء أو تغيير معرّفاتها.
- إدراج الصفوف يحتفظ بالتنسيق والتحقق من البيانات والأكواد النصية، ولا يتأثر بالصفوف المجهزة بمربعات اختيار FALSE.
- مسار `pull` موثّق بالكود ويرفض كتابة قيم الهاتف؛ يقرأ المساحة الخاصة بالأخصائية فقط.
- الهاتف يرسل السجلات المعدلة والجديدة، ويتوقف عن إعادة إرسال سجلات لم تتغير منذ آخر تأكيد.
- جلب تلقائي كل 15 ثانية أثناء فتح التطبيق، ومحاولات رفع مباشرة بعد الحفظ وعودة الشبكة، مع إعادة محاولة محدودة وجدولة خلفية.
- عرض «جلب تعديلات الشيت» و«رفع تعديلات الهاتف» وحالة المزامنة والخطأ وعدد الملفات التي أكدها الشيت.
- الجلب يحافظ على الأسماء المحلية غير المرسلة والتعديلات المعلقة، ويعرض التعارض عند تعديل الحقل نفسه من الطرفين.
- الدخول بالكود فقط والرابط المضمن وشعار غرس من التحديث السابق مشمولة في الحزمة.
- الإصدار 1.1.1، `versionCode=5`. الحزمة الأساسية لا يتغير معرفها `ps.ghars.sessions`، والتحديث المباشر يحتاج مفتاح التوقيع الموافق للمثبت.
- بناء اختياري منفصل في نفس سير العمل بمعرف `ps.ghars.sessions.transfer` واسم «أخصائية غرس · المزامنة»، لتجربة نقل نسخة احتياطية مع إبقاء القديم مثبتًا.
- مخرجات GitHub القابلة للتثبيت تتطلب مفتاحًا دائمًا؛ لا يُوزع Debug بمفتاح مؤقت. أضيف سكربت Windows لإنشاء مفتاح مرة واحدة دون الكتابة فوق مفتاح موجود.
- لم تتغير `SecureStore` أو صيغة النسخة الاحتياطية، ولا تُحذف أسماء المعلمة تلقائيًا. حفظ النسخة الاحتياطية خارج التطبيق والتحقق على الهاتف مطلوبان قبل اعتماد التحديث.

التفاصيل وخطوات النشر: `START-HERE-AR.md`.
