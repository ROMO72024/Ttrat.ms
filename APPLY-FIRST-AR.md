# تطبيق تصحيح GitHub

هذه الحزمة تعدّل طريقة بناء الإصدارات القادمة بمفتاح دائم. لا تستعيد مفتاح Debug القديم ولا تجعل النسخة الجديدة تحديثًا مباشرًا له. ثبت بالفحص أن APK القديم والجديد لهما معرف واحد وتوقيعان مختلفان، وأن المفتاح القديم لم يُحفظ في مخرجات تشغيله.

تمكنت من قراءة المستودع والتشغيلات، لكن اتصال GitHub رفض الكتابة برسالة Resource not accessible by integration. لذلك لم أنشئ فرعًا أو طلب تعديل، ولم أغيّر المستودع.

## استبدال الملفات الثلاثة

1. فك ضغط الحزمة.
2. افتح الرابط التالي في GitHub:
   https://github.com/ROMO72024/Ttrat.ms/edit/main/.github/workflows/main.yml
3. افتح الملف `.github/workflows/main.yml` من الحزمة في المفكرة، وانسخ محتواه كاملًا مكان محتوى المحرر على GitHub، ثم Commit changes. هذه أهم خطوة؛ رفع ملفات التطبيق وحدها يترك إعداد البناء القديم يعمل.
4. ارفع الملف `docs/APK-UPDATE-AR.md` إلى مجلد docs في المستودع، واستبدل README.md بالمرفق.
5. اتبع دليل `docs/APK-UPDATE-AR.md`: نسخة احتياطية من القديم، ثم حفظ مفتاح توقيع دائم في الأسرار الأربعة، ثم Actions ← Build stable APK ← Run workflow.

معنى ظهور تشغيل ناجح بعد رفع التعديلات: نجحت الفحوص فقط. بناء ملف التثبيت الموقّع يتم عند الضغط على Run workflow يدويًا بعد وضع أسرار التوقيع.

المشروع الحالي يحتوي أداة `tools/create-signing-key.ps1` لإنشاء مفتاح دائم مرة واحدة على الكمبيوتر. إذا كانت لديك أسرار مفتاح دائم مسبقًا، أعد استخدامه. لا تولد مفتاحًا جديدًا لكل إصدار.

سيظهر في Artifacts ملف ghars-stable-apk، وداخله ghars-stable.apk. التطبيق المستقر يعمل بجانب القديم، وتستعيد فيه النسخة الاحتياطية مرة واحدة. تحديثاته المستقبلية تستخدم معرّفه ومفتاحه نفسيهما مع زيادة رقم الإصدار.

لا تحذف التطبيق القديم أو تمسح بياناته قبل التحقق من عدد الطلاب والخطط والجلسات والحضور في التطبيق المستقر. لم تتم تجربة الاستعادة على بيانات المعلمة الفعلية بعد.
