# غرس 1.1.0 — ابدأ من هنا

> دليل تاريخي لتهيئة 1.1.0. للنسخة الحالية 1.1.1 استخدم `START-HERE-AR.md`: تغيّر موضع إدراج الطلاب، وزر الجلب، وسير بناء APK، وأصبح الرابط مضمنًا والدخول بالكود فقط.

هذه نسخة معدّلة من مشروع Android الذي أرسلته، وليست نسخة نظام الحجز القديم بـ Apps Script. الحزمة تحتوي المصدر كاملاً وكود Google Sheets. لا تحتوي APK جاهزاً أو مفتاح توقيع خاصاً.

## ١. قبل أي تحديث: احمِ أسماء المعلمة

1. افتح **التطبيق الموجود حالياً على هاتف المعلمة**.
2. من الإعدادات اختر **نسخة احتياطية مشفرة**. ضع كلمة مرور تحفظها، واحفظ ملف `.ghars` خارج مساحة التطبيق، وانسخه إلى كمبيوتر أو مكان خاص موثوق.
3. احتفظ بملف APK القديم والمشروع القديم. سجل عدد الطلاب والجلسات، وتحقق من إمكانية قراءة النسخة على جهاز تجريبي إن أمكن.
4. لا تحذف التطبيق، ولا تضغط «مسح البيانات»، ولا تستعد نسخة قديمة فوق البيانات الحالية للتجربة.

### هل سيفتح APK الجديد كتحديث؟

فقط إذا تطابق **معرّف الحزمة ومفتاح توقيعها** مع المثبّت، وكان رقم الإصدار أعلى. أبقيت حزمة release كما هي `ps.ghars.sessions`، وحزمة debug كما هي `ps.ghars.sessions.debug`، ورفعت `versionCode` من **3 إلى 4**، و`versionName` إلى **1.1.0**. لم أغيّر ملف البيانات `state.ghl` أو مفتاح تخزينها `ghars.device.data.v1` أو صيغة قاعدة البيانات `version: 1`.

| النسخة المثبّتة | مسار التحديث |
| --- | --- |
| `ghars-signed-release-apk` | ابنِ release باستخدام **نفس أسرار التوقيع القديمة**. لا تنشئ مفتاحاً جديداً. |
| `ghars-debug-apk` من GitHub | مفتاح debug في المشروع الأصلي قد يتغير بين عمليات البناء؛ لا نفترض أن التحديث سيقبل. يلزم مفتاح debug الأصلي، أو نقل آمن بواسطة نسخة احتياطية إلى release منفصلة. |
| نسخة preview أو APK من مصدر آخر | يلزم معرفة معرّفها وتوقيعها. هذه الحزمة لا تحتوي مفتاح معاينة قديم. |

إذا ظهر «التطبيق غير مثبت» أو «تعارض في الحزمة»، **توقف ولا تحذف التطبيق القديم**. إذا ظهر تطبيق ثانٍ فارغ فهذا غالباً اختلاف الحزمة، وليس اختفاء الأسماء من التطبيق الأول. اسم الأيقونة وحده لا يكفي لتحديد النسخة. [مرجع Android للتوقيع](https://developer.android.com/studio/publish/app-signing)، [وترقيم الإصدارات](https://developer.android.com/studio/publish/versioning).

## ٢. جهّز Google Sheets — ملف جديد مستقل

1. افتح **Google Sheets جديداً** وسمّه مثلاً «غرس — الأخصائيات والجلسات».
2. لا تحذف أوراق النظام القديم؛ هذا الربط مستقل عنها. اترك الملف الجديد خاصاً، ولا تنشره على الويب.
3. اختر **الإضافات / Extensions → Apps Script**.
4. افتح الملف `google-apps-script/Code.gs` من هذه الحزمة باستخدام محرر نصوص.
5. انسخ محتواه بالكامل إلى `Code.gs` في **المشروع الجديد فقط**. احفظ.
6. من قائمة الدوال اختر `setupGharsSync`، ثم **تشغيل / Run**. وافق على الأذونات لحساب المدرسة الذي ستنشر منه السكربت.
7. عند ظهور رسالة إتمام التهيئة، ارجع إلى الشيت وحدّث الصفحة.

ستنشأ **٣ أوراق فقط يحتاجها النظام**:

| الورقة | وظيفتها | هل تعدّلها يدوياً؟ |
| --- | --- | --- |
| الأخصائيات | اسم الأخصائية، كود الدخول، حالة الحساب، ومعرّف ثابت مخفي | نعم: الاسم والكود والحالة فقط |
| طلاب غرس | إدخال الطلاب والخطط، وعرض المنجز والمتبقي | نعم: حقول التسجيل، وليس الأعمدة التلقائية |
| بيانات التطبيق | سجل تقني للطلاب والجلسات والملاحظات، لمنع التكرار ودعم الهواتف المتعددة | لا. تُخفى لتقليل الازدحام وليست مخفية أمنيّاً |

قد تبقى ورقة Google الافتراضية الفارغة؛ السكربت لا يحذف أي ورقة، ولا يلزم حذفها. إعادة تشغيل التهيئة لا تمسح الأسماء ولا تغيّر الأكواد أو معرّفات المساحات.

ملف `google-apps-script/appsscript.json` مرفق لضبط توقيت المشروع إلى `Asia/Gaza` وتشغيل V8. يمكن إظهار ملف البيان من إعدادات مشروع Apps Script ونسخ محتواه، أو ضبط التوقيت من إعدادات المشروع. لا يلزم إنشاء مشروع سحابي مدفوع أو تفعيل واجهة Google Sheets API يدوياً.

## ٣. أضف أكواد الأخصائيات

في ورقة **الأخصائيات** اكتب في صف جديد:

| اسم الأخصائية | كود الدخول | حالة الحساب |
| --- | --- | --- |
| اسم المعلمة الحالية | اختر كوداً خاصاً بها | مفعّل |
| اسم أخصائية أخرى | اختر كوداً مختلفاً | مفعّل |

يكفي الاسم والكود؛ الحالة الفارغة تعني مفعّل. اختر «موقوف» لإيقاف حساب. لا يوجد كود جاهز أو حساب افتراضي في السكربت.

- الأكواد في الشيت **نص واضح وليست مشفرة أو محوّلة إلى hash**. يقبل الكود من **٥ إلى ٣٢ حرفاً إنجليزياً أو رقماً**، وتوجد حساسية لحالة الحروف. يمكن استخدام ٦ أو ٧ أرقام كما طلبت. يفضّل كود أطول غير متوقع.
- عمود الأكواد منسّق كنص ليحافظ على الصفر في بداية الكود. أدخل الأرقام الإنجليزية `0–9`.
- لا تستخدم الكود نفسه لأخصائيتين مختلفتين؛ نفس الكود يعني نفس المساحة بكل صلاحياتها.
- لا تعدّل المعرّف المخفي أو تنسخ صف حساب كاملاً. أضف الاسم والكود فقط في صف جديد؛ يتولد المعرّف تلقائياً عند قراءة الحسابات.
- لتغيير كود أخصائية: عدّل **كود صفها نفسه**، ولا تحذف الصف وتعيد إنشاءه. تبقى هوية المساحة والأسماء والجلسات.
- المحاسب الذي أعطيته صلاحية تعديل هذا الشيت يستطيع إضافة الأكواد وتغييرها. لا توجد حاجة لمرور كل إضافة بك أنت.

**تنبيه صريح:** الكود البسيط ليس حماية قوية؛ كل من يعرفه يملك صلاحية كاملة على مساحة الأخصائية. يوجد حد محدود للمحاولات من الجهاز، لكنه ليس نظام مكافحة تخمين قويّاً. لا تستخدم أكواداً معروفة للجميع ولا تشاركها في الصور العامة.

## ٤. انشر كود الربط

في Apps Script:

1. اختر **نشر / Deploy → عملية نشر جديدة / New deployment**.
2. اختر النوع **تطبيق ويب / Web app**.
3. **التنفيذ باسم / Execute as: أنا / Me**، أي حساب المدرسة المالك للسكربت.
4. **من لديه حق الوصول / Who has access: أي شخص / Anyone**. هذا إذن استدعاء بوابة الكود؛ **لا تجعل ملف Google Sheets نفسه عاماً**.
5. انشر، ثم انسخ **رابط تطبيق الويب الذي ينتهي بـ `/exec`**.

إذا كانت سياسة حساب Google Workspace تمنع هذا النشر، توقف واستشر مسؤول الحساب؛ لا تتجاوز سياسة المدرسة ولا تغيّر الأذونات العامة للشيت لمعالجة ذلك.

فتح رابط `/exec` في المتصفح يظهر رسالة JSON صغيرة، وليس واجهة التطبيق أو أسماء الطلاب. هذا طبيعي: الواجهة داخل APK، والسكربت بوابة مزامنة تتحقق من الكود.

لا تستخدم رابط محرر Apps Script، أو رابط ملف الشيت، أو رابط الاختبار المنتهي بـ `/dev`. وعند تحديث السكربت لاحقاً اختر **إدارة عمليات النشر → تعديل النشر الحالي → إصدار جديد → نشر** للحفاظ على نفس الرابط. [شرح Google الرسمي](https://developers.google.com/apps-script/guides/web).

## ٥. ضع رابط المدرسة في المشروع مرة واحدة

افتح:

`app/src/main/assets/sync-config.json`

واستبدل القيمة الفارغة بالرابط الحقيقي، مع إبقاء JSON صالحاً:

```json
{
  "url": "https://script.google.com/macros/s/ضع_معرف_النشر_الحقيقي_هنا/exec"
}
```

المثال ليس رابطاً جاهزاً؛ يجب استخدام الرابط المنسوخ من Google. بعد ذلك كل من يثبت APK يحتاج **كود الأخصائية فقط**، دون إعداد رابط المدرسة على كل هاتف. الرابط ليس مفتاحاً سرياً، ولا تضع أكواد الأخصائيات داخل ملفات GitHub.

إذا تركت الرابط فارغاً، ستظهر خانة «رابط المدرسة» عند الدخول لإعداده يدوياً على الهاتف. ويمكن تعديل الرابط من شاشة الدخول. الأفضل تضمينه قبل البناء لتبقى الشاشة بسيطة.

لا ترفع `App.html` أو `Index.html` من نظام غرس القديم إلى Apps Script الجديد. لهذا التطبيق يكفي كود الربط؛ ملفات HTML الفعلية موجودة داخل مشروع Android.

## ٦. ارفع التعديل إلى مستودع GitHub نفسه وابنِ APK

1. احفظ نسخة من المستودع القديم أو أنشئ فرعاً/وسماً للإصدار الحالي قبل الاستبدال.
2. فك الحزمة، ثم ارفع **محتويات مجلد `Ttrat.ms-main`** إلى جذر المستودع، لا ZIP نفسه ولا مجلداً إضافياً داخله.
3. حافظ على ملف التشغيل `.github/workflows/main.yml`، وملفات Gradle، وبقية الملفات المرفقة. لا تستبدل `Code.gs` وحده داخل Android؛ التحديث يشمل Java والواجهة وملف البيان.
4. لا تغيّر أسرار التوقيع القديمة. إذا كانت release الحالية موقّعة، افتح **Actions → Build APK → Run workflow** وفعّل `signed_release`.
5. انتظر نجاح اختبارات Node، ثم `lintDebug` و`testDebugUnitTest` والبناء. حمّل `ghars-signed-release-apk` للنسخة الدائمة الموقّعة.
6. إن فشل البناء، أرسل رسالة الخطأ الفعلية؛ لا تثبّت إصداراً آخر أو تحذف تطبيق المعلمة لمحاولة حل خطأ البناء.

لم أضف مفاتيح توقيع أو بيانات حقيقية للمشروع. **لا تستخدم تعليمات إنشاء مفتاح جديد من `docs/SIGNING.md` إذا كنت تملك مفتاح النسخة الحالية بالفعل**؛ استخدم المفتاح نفسه. إنشاء مفتاح جديد يفيد نسخة دائمة جديدة، ولا يجعلها تحديثاً لتطبيق موقّع بمفتاح آخر.

## ٧. ثبّت التحديث على هاتف المعلمة وارفع الأسماء القديمة

بعد التحقق من الحزمة والتوقيع والنسخة الاحتياطية:

1. افتح APK الجديد على الهاتف **دون حذف التطبيق القديم**. يفترض أن يعرض Android تثبيت تحديث للتطبيق الموجود.
2. افتح التطبيق مع توفر الإنترنت، وأدخل **كود المعلمة صاحبة الأسماء الحالية**.
3. يظهر اسم مساحة الأخصائية وعدد الطلاب القدامى. اقرأ الاسم جيداً. إن كان خطأ، اضغط تراجع وادخل الكود الصحيح.
4. عند التأكيد تبقى الأسماء والجلسات محلياً، ويُحفظ ملف أمان داخلي قبل ربط البيانات. هذا لا يغني عن النسخة المصدّرة خارج الجهاز.
5. افتح **الإعدادات → رفع الأسماء الحالية وتفعيل المزامنة**. أكد الرفع بعد مراجعة اسم الأخصائية.
6. انتظر ظهور تاريخ **آخر مزامنة ناجحة**. إن انقطع الإنترنت تبقى البيانات محفوظة، وتُعاد المحاولة؛ لا تعِد إنشاء الطلاب.
7. افتح الشيت وتأكد من العدد ومن بضعة أسماء وأرصدة وجلسات. لا تكتب الأسماء القديمة يدوياً قبل أول رفع، ولا تضغط «استعادة نسخة» لغرض الرفع؛ الاستعادة عملية مختلفة تستبدل بيانات الهاتف.

زر الرفع هنا **تصدير من الهاتف إلى الشيت**، وليس مسحاً أو نقلاً يزيل الأصل. تكرار المزامنة يحافظ على معرّفات الطلاب والجلسات؛ لا يضيف صفاً جديداً لكل محاولة. لا ندمج شخصين اعتماداً على الاسم فقط، فقد يتشابه اسما طفلين.

## ٨. جرّب هاتفك وكود أخصائية أخرى

1. ثبّت APK على هاتفك. أدخل **نفس كود المعلمة**. انتظر المزامنة؛ يظهر نفس سجل الطلاب والجلسات والحضور.
2. لا تستورد نسخة احتياطية يدوياً على الهاتف الثاني بعد أن تزامنت البيانات؛ يكفي الكود.
3. عدّل حقلاً بسيطاً على الهاتف الثاني، ثم زامن وتحقق من ظهوره على الأول.
4. سجل الخروج، وأدخل كود الأخصائية الثانية. تفتح مساحة مستقلة، ولا تُنقل أسماء الأولى إليها.
5. العودة إلى كود سابق تفتح ذاكرته المحلية الخاصة. الملفات مفصولة بمعرّف المساحة الثابت، لا بالكود القابل للتغيير.

التذكيرات الشخصية وإعدادات قفل الجهاز والصوت محلية لكل هاتف. الجلسات نفسها مشتركة، ولذلك قد يصدر تنبيه الجلسة على كلا الهاتفين إذا فُعّلت أذوناتهما. على الهاتف الذي لا يحتاج المنبه اضبط أذوناته/قناته من إعدادات Android. عند تبديل الأخصائية على هاتف واحد، تُجدول منبهات المساحة النشطة فقط.

## ٩. عمل المحاسب اليومي

من ورقة **طلاب غرس**، في صف جديد، اكتب:

| كود الأخصائية | اسم الطالب الثلاثي | إجمالي جلسات النطق | إجمالي جلسات السلوك |
| --- | --- | --- | --- |
| الكود الصحيح | اسم الطالب | 20 | 0 |

- إذا كانت الاشتراكات سلوكية، ضع العدد في السلوك بدلاً من النطق. هاتان خطتان منفصلتان كما في التطبيق الأصلي.
- أكمل ما تعرفه من الصف والهاتف. تكمل الأخصائية باقي البيانات والخطة الأسبوعية من التطبيق.
- **الإجمالي هو كامل الرصيد المسجل، وليس آخر دفعة فقط.** إذا كان الإجمالي 20 واشترى 10 إضافية، يصبح الإجمالي 30. لا يوجد دفتر مدفوعات أو فواتير مالية في هذا التحديث.
- «المنفذ» يحسب الحضور مرة واحدة من الجلسات المشتركة، و«المتبقي» هو الإجمالي ناقص المنفذ بحد أدنى صفر. الغياب لا يستهلك جلسة. تُحدّث هذه الأعمدة عند المزامنة، وليست فورية أثناء انقطاع هاتف الأخصائية عن الإنترنت.
- لا تغيّر المعرّفات الثابتة، ولا تنسخ صفاً كاملاً بمعرّفاته المخفية. لا تفرز عمود الاسم وحده؛ فرز الصفوف يجب أن يشمل كل الأعمدة. تجنب إعادة ترتيب الورقة أثناء الرفع الأول.
- لا تحذف صف طالب لإنهاء متابعته؛ فعّل **مؤرشف**. حذف الصف يدوياً قد يعيده نظام المزامنة من السجل. إلغاء جلسة يتم من التطبيق وتبقى في التاريخ.
- كود الأخصائية في صف طالب موجود يعرّف العرض الحالي، وليس أداة نقل الطالب إلى مساحة أخرى. نقل ملكية ملف بين أخصائيتين ليس عملية تلقائية في هذا الإصدار.

## ١٠. ماذا يحدث دون إنترنت أو عند التعارض؟

- أول دخول بكود جديد يحتاج الإنترنت. بعد نجاح الدخول يمكن فتح المساحة المحفوظة بالكود نفسه والعمل عليها دون إنترنت.
- إنشاء الطلاب والجلسات وتسجيل الحضور والملاحظات والبحث والتذكير المحلي لا يعتمد على نجاح المزامنة.
- بعد التعديل تُجدول محاولة شبكة. وأثناء فتح التطبيق يجري فحص كل دقيقة؛ وفي الخلفية يستخدم Android مهمة دورية لا تقل عن 15 دقيقة ومحاولات مؤجلة عند انقطاع الشبكة. هذه **جدولة وليست وعداً بوقت تنفيذ محدد**؛ توفير البطارية وفرض الإيقاف قد يؤخران التنفيذ. افتح التطبيق أو اضغط «مزامنة الآن» عند الحاجة. [مرجع Android لجدولة المهام](https://developer.android.com/reference/android/app/job/JobInfo.Builder).
- اختلاف حقلين مستقلين يُدمج تلقائياً. إذا عدّل الهاتف والمحاسب أو هاتفان **نفس الحقل** من نسختين مختلفتين، يُحفظ الاختلاف للمراجعة من إعدادات الربط. اختر القيمة الصحيحة؛ لا نستخدم ساعة الجهاز لحسم الخلاف.
- إذا أنشأ هاتفان جلستين متداخلتين أثناء الانقطاع، لا نحذف أحد الموعدين؛ يظهر تنبيه «مواعيد متداخلة» لمراجعتهما وتعديل/إلغاء الموعد المناسب.
- تغيير الكود أو إيقاف الحساب يمنع الدخول والمزامنة بالكود القديم عند الاتصال. **لا يستطيع النظام سحب البيانات من هاتف غير متصل**؛ تظل نسخته المحلية قابلة للوصول بالكود الذي تحقق منه سابقاً ما لم يكن قد تلقى الإيقاف بالفعل.
- إذا سُجّل الخروج تتوقف المزامنة لذلك الهاتف، وتبقى البيانات المحلية محفوظة حتى الدخول مجدداً.

## ١١. الخصوصية وحدود هذا الحل

- ملف الشيت يضم أسماء الأطفال ومعلومات المتابعة. اجعل المشاركة مقتصرة على حسابك وحساب المحاسب الموثوق. المستخدمون الآخرون يدخلون التطبيق بالكود، ولا يحتاجون مشاركة الشيت معهم.
- حماية بعض الأعمدة تمنع العبث العرضي؛ إخفاء الورقة التقنية **لا يمنع قراءتها** ممن يستطيع الوصول إلى الملف. كذلك محررو الشيت المربوط قد يستطيعون الوصول إلى مشروع السكربت.
- الدخول برمز بسيط كما طلبت لا يعادل حساباً شخصياً موثقاً، ولا يوفر تدقيقاً موثوقاً لهوية مَن عدّل من حاملي الكود نفسه.
- حماية التخزين المحلي والنسخ الاحتياطية الأصلية بقيت كما هي. طلبك بوضوح الأكواد ينطبق على أكواد الشيت؛ لم أزل حماية ملفات الأطفال المحفوظة على الهاتف.
- الحل لا يحتاج n8n أو اشتراكاً إضافياً في بنائه الحالي، لكنه يخضع لحدود وحصص Google وقيود Android. ليست هناك ضمانة استضافة مجانية غير محدودة.
- الحد لكل مساحة: 5000 طالب و30000 جلسة؛ طلب/رد المزامنة لا يزيد على 5 MiB، وسجل واحد لا يتجاوز نحو 45000 حرف JSON بسبب خلايا Sheets. البيانات الكبيرة أو الملاحظات المطولة جداً تحتاج تطويراً لاحقاً؛ تظهر رسالة خطأ ويبقى العمل المحلي محفوظاً.
- Sheets ليست قاعدة بيانات ذات معاملات تمنع تحرير البشر أثناء كتابة السكربت. نستخدم قفلاً للسكربت، ومعرّفات ثابتة، ودمجاً حسب الحقول، وفحصاً قبل الكتابة، لكن تجنب تعديل/فرز نفس الصف لحظياً أثناء المزامنة الحساسة. احتفظ بنسخ احتياطية.
- نقل نسخة محفوظة إلى هاتف آخر بالتصدير والاستعادة يتم بعد التأكيد. الاستعادة توقف المزامنة وتزيل سجل المقارنة السابق؛ إعادة تفعيلها تدمج مع سجلات المساحة الموجودة، ولا تمسح سجل الشيت لتطابق نسخة أقدم.

## ١٢. اختبار القبول قبل الاعتماد على بيانات المعلمة

ابدأ بكود أخصائية تجريبية وطلاب وهميين. اختبر هاتفين: إضافة محاسب، إضافة هاتف، حضور جلسة، انقطاع الإنترنت وإعادة فتح التطبيق، إعادة الاتصال، تعارض حقل واحد، تغيير الكود، إيقافه، ثم التبديل بين أخصائيتين. تحقق من أن الحساب الثاني لا يعرض أسماء الأول.

بعد نجاح البناء والتجربة، اختبر تثبيت التحديث فوق نسخة مطابقة من التطبيق القديم مع نسخة بيانات تجريبية. راجع بقاء أسماء الطلاب والجلسات والتذكيرات وعدّاد الرصيد. لا تعتمد على نجاح اختبارات الشيفرة وحده لإثبات التثبيت أو الرنين على هاتف فعلي.

نتيجة تجهيز هذه الحزمة: **34 اختبار Node ناجحاً**. أُضيفت اختبارات Java للدمج لتشغيلها في GitHub. لم يُبنَ APK هنا، ولم يجرِ نشر السكربت على حسابك أو اختبار هاتف فعلي؛ تنزيل Gradle غير متاح في بيئة التجهيز. انظر `docs/TESTING.md`.

## الخطوة التي تبدأ بها الآن

احفظ نسخة بيانات المعلمة، ثم نفّذ الأقسام **٢ → ٣ → ٤**: إنشاء الشيت، لصق `Code.gs`، التهيئة، إضافة كود المعلمة، ونشر رابط `/exec`. بعد ذلك ضع الرابط في `sync-config.json`، وتحقق من نوع APK وتوقيع النسخة الحالية قبل بناء التحديث وتثبيته.
